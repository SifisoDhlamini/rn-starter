import React from "react";
import { Text, StyleSheet } from "react-native";

const HomeScreen = () => {
  return <Text style={styles.style}>Student Buddy</Text>;
};

const styles = StyleSheet.create({
  style: {
    fontSize: 30
  }
});

export default HomeScreen;
